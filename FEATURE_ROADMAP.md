# AI 周报系统功能规划

> 基于当前系统能力，按优先级和实现难度分层列出可扩展功能。

---

## P0 - 高价值 / 低难度（建议优先实现）

### 1. 发送失败自动重试与告警

- 邮件发送失败时自动重试（最多 3 次，间隔 30s/60s/120s）
- 全部重试失败后，通过备用渠道告警（企业微信/钉钉 Webhook）
- 日志中记录失败原因和重试次数

### 2. 周报历史归档与检索

- 所有周报按周归档到 `reports/archive/YYYY-WW/` 目录
- 提供 `--search "关键词"` 命令在历史周报中全文检索
- 生成 `reports/index.json` 索引（日期、文件名、摘要）

### 3. 多格式输出（PDF / Word）

- `--format pdf`：Markdown → PDF（使用 `weasyprint` 或 `pandoc`）
- `--format docx`：Markdown → Word（使用 `python-docx`）
- 邮件附件自动适配格式（领导可能更需要 PDF）

### 4. 执行状态通知

- 任务执行完毕/失败时，发送简短状态消息到企业微信群（Webhook）
- 成功：`✅ 周报已生成并发送至 3 位收件人（耗时 45s）`
- 失败：`❌ 周报生成失败：AI API 超时（已重试 3 次）`

---

## P1 - 高价值 / 中难度

### 5. 周报环比对比

- 自动读取上周周报，与本周内容做对比
- 在周报末尾追加「本周变化」章节：
  - 新增任务
  - 完成任务（上周有本周无）
  - 持续进行任务
- Prompt 中注入上周内容，让 AI 生成对比分析

### 6. 多收件人差异化内容

- 配置中为不同收件人设定详细级别：
  - 领导：仅项目级摘要（5 条以内）
  - 同事：完整任务列表
- 一封邮件按收件人生成不同正文，或拆分为多封邮件

### 7. 项目进度追踪

- 维护 `projects.json`，记录每个项目的状态（进行中/已完成/阻塞）
- 周报中自动标注项目状态变化
- 连续 N 周出现在周报中的任务自动标记为「长期任务」

### 8. 工时数据可视化

- 解析 CRM 下载的 Excel 中的工时数据
- 生成简单的工时分布图（按项目/按天）
- 图表以图片形式嵌入邮件正文
- 依赖：`matplotlib` 或 `plotly`（生成 PNG 嵌入）

---

## P2 - 中价值 / 中难度

### 9. 报告审批流程

- 生成周报后不直接发送，先标记为「草稿」
- 通过企业微信/邮件通知指定审批人
- 审批人回复「通过」后自动发送，回复「修改: xxx」后重新生成
- 需要引入简单的状态机（draft → approved → sent）

### 10. Web 管理面板

- 轻量 Flask/FastAPI 面板：
  - 查看历史周报
  - 手动触发生成
  - 修改配置（provider、收件人等）
  - 查看执行日志
  - 上传 Excel 文件（替代手动放文件）
- 适合多人协作场景

### 11. 周报历史版本对比

- 支持将本周周报与上周周报并排对比，突出变化点
- 便于复盘每周工作增量

### 12. 智能任务分类

- AI 自动将任务按类型分类：
  - 开发 / 测试 / 会议 / 文档 / 沟通
- 按分类统计工时占比
- 在周报中展示分类饼图或统计表

---

## P3 - 锦上添花 / 高难度

### 13. 自然语言查询

- 支持 `python weekly_report.py --query "上周做了哪些测试工作？"`
- 从历史周报 + Excel 原始数据中检索回答
- 需要本地向量数据库（如 `chromadb`）

### 14. 自动生成下周计划

- 基于本周未完成任务 + 项目进度，AI 自动生成下周计划
- 周报末尾追加「下周计划」章节
- 可配置是否包含此章节

### 15. 多语言周报

- 支持 `--lang en` 生成英文周报
- 适用于跨国团队或外企汇报场景
- Prompt 模板按语言切换

### 16. CRM 数据自动同步

- 除 Excel 下载外，直接调用 CRM API 获取结构化任务数据
- 省去 Excel 解析步骤，数据更准确
- 可获取任务状态、优先级、负责人等额外字段

### 17. 定时报告摘要推送

- 每周一早上 9:00 先推送「数据准备完成」通知
- 10:00 生成周报后推送「周报已发送」通知
- 周五下午推送「本周数据预览」（提前感知工作量）

---

## 技术债务 / 改进项

| 项目 | 说明 |
|---|---|
| 单元测试 | 核心模块（日期计算、去重、prompt 构建）缺少测试 |
| 配置校验 | `load_config()` 后增加 schema 校验，提前报错 |
| 日志分级 | 当前全部用 `print`，建议引入 `logging` 模块 |
| 异步下载 | CRM 下载 + LLM 调用可用 `asyncio` 并行（大文件或慢网络时） |
| Docker 化 | 提供 `Dockerfile`，便于部署到服务器/NAS |
| CI/CD | GitHub Actions 自动运行测试 + lint |
| 密钥轮换提醒 | CRM token 过期前主动提醒（而非等到 401） |

---

## 实现建议

1. **P0 先做**：重试机制 + 状态通知 + PDF 输出，这三个对日常使用体验提升最大
2. **P1 按需**：环比对比和工时可视化是汇报亮点，领导爱看
3. **P2 看团队规模**：单人使用不需要审批流程和 Web 面板
4. **P3 远期规划**：自然语言查询和自动计划是 AI 深度应用方向